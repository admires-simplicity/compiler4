// if statement returning value with block sub-exprs

f(x : int) -> int = {
  if x = 0 then {
    printf("roger roger, it's zero");
    x
  } else {
    x * 2
  }
}

printf("%d\n", f(0));
printf("%d\n", f(1));
printf("%d\n", f(2));