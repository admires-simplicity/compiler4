f : (x : int) -> int = {
  if x < 1: 0
  else {
    if x = 1: 1
    else fib(x-1) + fib(x-2)
  }
}

printf "fib 0 is %d\n" (f 0);
printf "fib 1 is %d\n" (f 1);
printf "fib 2 is %d\n" (f 2);
printf "fib 3 is %d\n" (f 3);
printf "fib 4 is %d\n" (f 4);
printf "fib 5 is %d\n" (f 5);
